# Worker (Phase 0 skeleton)

Background job processor using **pg-boss** on PostgreSQL.

```bash
pnpm dev:worker
```

Requires `DATABASE_URL` (same as API). Phase 1+ will add bulk upload and notification jobs here.
